<<<<<<< HEAD
# Mission_Possible
=======
# Last day surprise (frontend only)

Minimal flow right now: **username → location → short confirmation** (all copy in `lib/content.ts`).

## Setup

```bash
cd Mission_Possible
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Production:

```bash
npm run build
npm start
```

## Where to edit copy

- **`lib/content.ts`** — headings, placeholders, errors, done screen text  
- **`lib/locationReaction.ts`** — optional jokes based on location keywords (does not change flow)
>>>>>>> d2afac9 (feat: mount SurpriseExperience on home page)
